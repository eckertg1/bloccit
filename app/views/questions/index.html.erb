<h1>All Questions</h1>

<% @questions.each do |f| %>
    <div class="media">
        <div class="media-body">
            <h4 class="media-heading">
                <%= link_to f.title, f %>
            </h4>
        </div>
    </div>
<% end %>
